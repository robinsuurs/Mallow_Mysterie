GridBackground {
    --grid-background-color: #282828;
    --line-color: rgba(193, 196, 192, 0.1);
    --thick-line-color: rgba(193, 196, 192, 0.1);
    --spacing: 10;
}

.textarea {
    display: flex;
    max-width: 300px;
    overflow: hidden;
    min-height: 40px;
    white-space: normal;
}